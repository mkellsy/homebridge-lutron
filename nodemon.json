{
    "watch": ["src"],
    "ext": "ts",
    "ignore": [],
    "exec": "tsc --project tsconfig.json && homebridge --debug --user-storage-path ./storage",
    "signal": "SIGTERM",
    "env": {
        "NODE_OPTIONS": "--trace-warnings"
    }
}
